# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'moviedb-mandiri' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for moviedb-mandiri
  pod 'Kingfisher'
  
  pod "youtube-ios-player-helper", "~> 1.0.2"
  
  target 'moviedb-mandiriTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'moviedb-mandiriUITests' do
    # Pods for testing
  end

end
